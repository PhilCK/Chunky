#include <chunky.h>
#include "chunky_ctx.h"
#include <stdlib.h>
#include <string.h>

struct chunky_recorder*
chunky_recorder_create(
        struct chunky_ctx *ctx)                     /* required */
{
        (void)ctx;
        (void)rec;
}

void
chunky_recorder_apply(
        struct chunky_ctx *ctx,                     /* required */
        struct chunky_recorder *rec)                /* required */
{
        (void)ctx;
        (void)rec;
}
